{
  "name": "xerus",
  "version": "0.0.1",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "build:types": "dts-bundle-generator --config dtsconfig.json",
    "dev": "bun --hot run ./src/index.ts"
  },
  "type": "module",
  "types": "src/index.ts"
}